name: release

on:
  workflow_dispatch:
    inputs:
      version:
        description: "version (e.g., v1.0.0)"
        required: false
        type: string
      build:
        description: "platform"
        required: true
        type: choice
        default: "all"
        options:
          - all
          - linux
          - windows
          - darwin
          - android
  schedule:
    - cron: "0 * * * *"

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ inputs.build }}
  cancel-in-progress: true

permissions:
  actions: write

env:
  UPSTREAM_REPO: SagerNet/sing-box
  UPSTREAM_REF: dev-next
  UPSTREAM_BUILD: /.github/workflows/build.yml
  UPDATE_RELEASE: false
  FUNC_WORKFLOW: |
    workflow_cancel()
    {
        curl -s "https://api.github.com/repos/${{ github.repository }}"\
    "/actions/runs/${{ github.run_id }}/cancel" \
             -X POST \
             -H "Accept: application/vnd.github+json" \
             -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
             -H "X-GitHub-Api-Version: 2022-11-28" >/dev/null 2>&1

        sleep ${1:-30}
        printf '%s\n' "🔄 Workflow cancellation timeout reached, forcing exit"
        exit 2
    }

    workflow_delete()
    {
        curl -s "https://api.github.com/repos/${{ github.repository }}"\
    "/actions/runs/${{ github.run_id }}" \
             -X DELETE \
             -H "Accept: application/vnd.github+json" \
             -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
             -H "X-GitHub-Api-Version: 2022-11-28" >/dev/null 2>&1

        sleep ${1:-30}
        printf '%s\n' "🔄 Workflow deletion timeout reached, forcing exit"
        exit 2
    }

jobs:
  compare_release:
    name: Compare release
    runs-on: ubuntu-latest
    outputs:
      upstream_name: ${{ steps.compare_release.outputs.upstream_name }}
      upstream_tag: ${{ steps.compare_release.outputs.upstream_tag }}
      local_name: ${{ steps.compare_release.outputs.local_name }}
      local_tag: ${{ steps.compare_release.outputs.local_tag }}
      matrix: ${{ steps.get_matrix.outputs.matrix }}
    steps:
      - name: Compare release
        id: compare_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          INPUT_VERSION: ${{ inputs.version }}
          FUNC_COMPARE_RELEASE: |
            get_release()
            {
                repo=$1
                tags=${2:+tags/}${2:-latest}
                curl -s "https://api.github.com/repos/${repo}/releases/${tags}" \
                     -H "Accept: application/vnd.github+json" \
                     -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
                     -H "X-GitHub-Api-Version: 2022-11-28" | \
                jq -crM '
                    (
                        select(.draft == false and .prerelease == false) |
                        .name, .tag_name, .published_at
                    ) // (
                        "None", "None", "None"
                    )
                ' 2>/dev/null || \
                printf '%s\n' "None" "None" "None"
            }

            vercomp()
            {
                case "$1" in
                    ""|.*|*.|*[!0-9.]*)
                        return 3
                        ;;
                esac

                case "$2" in
                    ""|.*|*.|*[!0-9.]*)
                        return 3
                        ;;
                esac

                [ "$1" = "$2" ] && exit 0

                ver1=$1 ver2=$2 pad=

                IFS=.
                set -- $ver1 $ver2
                while [ ${i:-0} -lt $# ]
                do
                    pad=$pad.0
                    : $(( i+=1 ))
                done

                set -- $ver2
                for v in $ver1 $pad
                do
                    unset IFS

                    if [ ${v:-0} -lt ${1:-0} ]
                    then
                        return 1
                    elif [ ${v:-0} -gt ${1:-0} ]
                    then
                        return 2
                    elif [ $# -eq 0 ]
                    then
                        return 0
                    fi
                    shift
                done
            }
        run: |
          . /dev/stdin << EOF
          $FUNC_WORKFLOW
          $FUNC_COMPARE_RELEASE
          EOF

          IFS="
          "
          set -- $(
              get_release "$UPSTREAM_REPO" "$INPUT_VERSION"
              get_release "${{ github.repository }}" "$INPUT_VERSION"
          )

          cat << EOF
          Event trigger: ${{ github.event_name }}

          ┌──────────────────────────────────────────────────────────────┐
          │ Release Comparison                                           │
          ├─────────────┬────────────┬────────────┬──────────────────────┤
          │ Repository  │ Name       │ Tag        │ Date                 │
          ├─────────────┼───────────-┼────────────┼──────────────────────┤
          │ Upstream    │ $( printf '%-10s │ %-10s │ %-20s │\n' "$1" "$2" "$3" )
          │ Local       │ $( printf '%-10s │ %-10s │ %-20s │\n' "$4" "$5" "$6" )
          └─────────────┴────────────┴────────────┴──────────────────────┘

          EOF

          if [ "$1" = "None" ]
          then
              printf '%s\n' "❌ Upstream release not found: ${INPUT_VERSION:-$1}"
              exit 1
          else
              vercomp "$1" "$4"
              ret=$?
              ! [ -z ${INPUT_VERSION+x} ]
              case "$(( $? + ret ))" in
                  0)
                      printf '%s\n' "🚀 Update release: $1"
                      UPDATE_RELEASE=true
                      ;;
                  1|2)
                      printf '%s\n' "✅ Already up to date"
                      workflow_cancel
                      exit 0
                      ;;
                  3|4)
                      printf '%s\n' "🚀 New release available: $1"
                      UPDATE_RELEASE=false
                      ;;
                  *)
                      printf '%s\n' "hello world=$ret"
                      UPDATE_RELEASE=false
                      ;;
              esac
          fi

          printf '%s\n' "upstream_name=$1" \
                        "upstream_tag=$2" \
                        "local_name=$4" \
                        "local_tag=$5" >> "$GITHUB_OUTPUT"

          printf '%s\n' "UPDATE_RELEASE=$UPDATE_RELEASE" >> "$GITHUB_ENV"
      - name: Get build matrix
        id: get_matrix
        env:
          INPUT_BUILD: ${{ inputs.build }}
        run: |
          UPSTREAM_BUILD=${UPSTREAM_REPO}/raw/${UPSTREAM_REF}${UPSTREAM_BUILD}

          if ! matrix=$(
              curl -fsSL "https://github.com/$UPSTREAM_BUILD" | \
              yq -rM '.jobs.build.strategy.matrix.include | @json' | \
              jq -e -crM --arg os "${INPUT_BUILD:-all}" '
                  if $os == "all" then . else map(select(.os == $os)) end |
                  if length > 0 then . else empty end
              '
          )
          then
              printf '%s\n' "❌ Upstream build not found: ${INPUT_BUILD:-all}"
              exit 1
          fi

          cat << EOF
          ┌─────────────────────────────────────────────────────────────────────────────────────────────────────────┐
          │ Build Matrix                                                                                            │
          ├────────────┬────────────────────────────────────────────────────────────────────────────────────────────┤
          │ os         │ arch                                                                                       │
          ├────────────┼────────────────────────────────────────────────────────────────────────────────────────────┤
          $(
              printf '%s' "$matrix" | \
              jq -r '
                  group_by(.os) |
                  sort_by(.[0].os) |
                  .[] |
                  "| " +
                  (.[0].os + " " * 10)[:10] +
                  " | " +
                  ([.[].arch] | unique | join(", ") | . + " " * 90)[:90] +
                  " |"
              '
          )
          └────────────┴────────────────────────────────────────────────────────────────────────────────────────────┘
          EOF

          printf '%s\n' "matrix=$matrix" >> "$GITHUB_OUTPUT"
      - name: Cleanup
        if: ${{ cancelled() && github.event_name == 'schedule' }}
        run: |
          . /dev/stdin << EOF
          $FUNC_WORKFLOW
          EOF

          workflow_delete
  build:
    name: Build binary
    runs-on: ubuntu-latest
    needs:
      - compare_release
    strategy:
      matrix:
        include: ${{ fromJson(needs.compare_release.outputs.matrix) }}
    steps:
      - name: Show build info
        run: |
          printf '%s\n' "🏗️ Building ${{ needs.compare_release.outputs.upstream_tag }} for ${{ matrix.os }}/${{ matrix.arch }}, ${UPDATE_RELEASE}"
